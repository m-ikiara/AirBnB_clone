# Unittests for AirBnB Clone

## What are they?

They are tests performed on the modules that make up the AirBnB Clone console. They will be run using the following command:

```sh
	python3 -m unittest discover tests
```
